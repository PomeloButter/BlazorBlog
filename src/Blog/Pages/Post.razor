@page "/Posts"
@page "/Posts/{PageIndex:int}"
@page "/Posts/Tags/{Tag}/{PageIndex:int}"
    <PostListBody Tag="@Tag" PageIndex="@PageIndex" />

@code
{
    [Parameter] public int PageIndex { get; set; }
    [Parameter] public string Tag { get; set; } 
}
